---
title: Core Initialization
---

# Core Initialization

The legacy `FeatherCore = exports['feather-core']:initiate()` pattern is no longer the public contract. Current Feather resources call named exports directly and receive structured result envelopes.

Start with the [Core resource reference](/api/resources/Core) for readiness, capabilities, sessions, providers, guards, RPC, locale, events, and notifications.
