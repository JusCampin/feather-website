---
title: Events (Core and World)
---

# Events

For application-level events, use Feather Core's declared event broker. Its public contracts are documented in the [Core resource reference](/api/resources/Core).

The event files inside `feather-world` are implementation details, not a public event API. World integrations should use the exports in the [World resource reference](/api/resources/World).
