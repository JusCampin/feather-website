---
title: DataView (World Internals)
---

# DataView

DataView helpers currently belong to `feather-world`'s internal compatibility layer. They are not a public cross-resource API.

Use the supported exports in the [World resource reference](/api/resources/World). If a feature truly requires raw DataView access, keep that implementation local to its owning resource.
